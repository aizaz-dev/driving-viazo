import hero from "./schemas/hero";
import features from "./schemas/features";
import page from "./schemas/page";
import drivers from "./schemas/drivers";

export const schema = {
  types: [hero, features, drivers, page],
};
